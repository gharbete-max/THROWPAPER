node_modules/
dist/
# The SSR bundle, built beside `dist` by `vite build --ssr`.
dist-server/
build/
coverage/
.env
.env.local
*.local
*.log
.DS_Store
.vite/
.turbo/
proof-out/

# generated documents (local DocumentStore stopgap)
.documents/

# playwright
test-results/
playwright-report/
blob-report/

# Playwright MCP scratch: page snapshots, console logs and screenshots it writes while driving
# the app. Debugging output, not source — and it lands in the repo root by default.
.playwright-mcp/

# design hook cache (generated)
.impeccable/hook.cache.json
