/**
 * The shared data grid (SPEC-shared.md §packages/ui) is Track A phase A4 and is deliberately NOT
 * in v0.1 — START-HERE.md says use a library for the submissions table in phase 3 instead.
 */
export { cn } from './cn.js';
