node_modules
dist
dist-server
apps/forms/public/ocr
coverage
pnpm-lock.yaml
docs

# generated by drizzle-kit
**/drizzle/
.playwright-mcp/

# The desktop edition's staging folder and installers (ADR 0016).
apps/desktop/.stage/
apps/desktop/release/

.impeccable/hook.cache.json
.impeccable/critique/

# Agent git worktrees. Each holds a FULL copy of the source, so every pattern above — which is
# anchored at the repo root — misses its copy: `apps/forms/public/ocr` does not match
# `.claude/worktrees/x/apps/forms/public/ocr`. Without this line, a vendored wasm file inside a
# worktree fails `format:check` for the whole repo. Do not remove; it is not redundant.
.claude/
